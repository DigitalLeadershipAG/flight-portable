---
description: Capture an open task or a longer memo into the flight workbench.
argument-hint: [task text or longer memo]
---

Follow the instructions in the `memo` skill (`.claude/skills/memo/SKILL.md`) to record the following as an open task or a longer memo:

$ARGUMENTS

If no text was provided above, ask the user what to record.

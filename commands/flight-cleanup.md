---
description: Strip closed and stale tasks from the flight open-task list, archiving what was removed.
---

Follow the instructions in the `cleanup` skill (`.claude/skills/cleanup/SKILL.md`): remove closed/stale tasks from `flight-workbench/memos/tasks-<user>.md` and archive the strippings under `flight-workbench/archive/`.

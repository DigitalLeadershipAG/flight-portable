---
description: Set up or refresh the flight workbench in the current folder, then show open tasks.
---

Follow the instructions in the `start` skill (`.claude/skills/start/SKILL.md`): initialize `./flight-workbench/` in the current folder, install the bundled style profiles, create `CLAUDE.md` only if it is missing, create today's session history file, and report open tasks. Never overwrite existing user content.
